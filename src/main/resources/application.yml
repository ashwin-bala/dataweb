management:
    metrics:
        tags:
            application: ${spring.application.name}
    endpoints:
        web:
            exposure:
                include: [ "prometheus" ]
amazonsqs:
    queueName: MessageQueue.fifo